---
title: Page not found
description: The page you were looking for does not exist.
template: splash
editUrl: false
# draft:true keeps this out of Starlight's generated routes in production (so it doesn't collide with the
# built-in /404 route), while Starlight's 404 handler still finds it via getEntry and renders it as the 404.
draft: true
---

That page doesn’t exist. Try the [documentation home](/start/introduction/), or head back to the
[Dispatch home page](/).
